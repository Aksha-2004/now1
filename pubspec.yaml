name: now
description: "A new Flutter project."
publish_to: 'none' # Prevent publishing to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # ✅ Firebase packages (Dart 3 compatible)
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  cloud_functions: ^5.1.4
  firebase_messaging: ^15.1.2

  # ✅ Other useful dependencies
  url_launcher: ^6.3.1
  geolocator: ^10.1.1
  geocoding: ^3.0.0
  intl: ^0.19.0
  nearby_connections: ^3.0.2
  flutter_sms: ^2.3.3
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mockito: ^5.4.4

flutter:
  uses-material-design: true
